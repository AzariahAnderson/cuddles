{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the main window. Grants no filesystem, shell, process or network access.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-app-info"]
}
