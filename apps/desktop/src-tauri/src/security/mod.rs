//! PermissionService (future). Scopes: read workspace, write workspace, execute command,
//! network, Git mutation, delete files, install packages, launch applications, computer control.
//! Every privileged action will pass through here and be logged. Not active in Phase 1.
